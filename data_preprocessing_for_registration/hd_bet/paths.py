import os

# please refer to the readme on where to get the parameters. Save them in this folder:
cur = os.path.split(os.path.abspath(__file__))[0]
folder_with_parameter_files = os.path.join(cur, "hd-bet_params")
